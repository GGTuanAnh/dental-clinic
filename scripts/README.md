Auto-push helper

Usage (Linux/macOS/Git Bash):

  chmod +x scripts/auto_push.sh
  ./scripts/auto_push.sh "your commit message"

On Windows PowerShell, run the git commands directly as shown in the project README.
